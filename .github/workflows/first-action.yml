name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print two steps
        run: |
            echo "First output"
            echo "Second outpout"
